User-agent: *
Allow: /
# Nothing under /api/ is reachable without a key, so every fetch of it is a refusal, and a
# refusal spends the same crawl budget as a page. The rows a renderer would have wanted are
# written into the page it is already holding — see api/shell.ts.
Disallow: /api/

Sitemap: https://fomopulse.app/sitemap.xml
